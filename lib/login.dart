import 'package:flutter/material.dart';
class LogInPagege extends StatelessWidget {
  const LogInPagege({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}